Docker Commands:

docker images
docker rmi 
docker ps 
docker stop <container-id>
docker ps -a
docker rm <container-id>
docker rm -f $(docker ps -a -q) 

docker build -f "C:\Prabhakar\Source\GitHub\dotnet-microservices\apps\Shopping.Api\Shopping.Api.Host\Dockerfile" -t shoppingapi .

dotnet --roll-forward Major /VSTools/DistrolessHelper/DistrolessHelper.dll --wait


Moving Local Image to Docker Hub:

docker login
docker tag <ImageId> <dockerhubReposirotyFullname> (docker tag bbb prabhakargvp/shoppingapi)
docker push <Image Name> (docker push prabhakargvp/shoppingapi:latest)


